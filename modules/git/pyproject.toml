[project]
name = "git-mcp"
version = "0.1.0"
description = "MCP Server for Git operations - Automated version control via MCP"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
git-mcp = "git_mcp.__main__:main"
